const fs = require('fs');

const first = fs.readFileSync('./data/first.txt', 'utf8');
const second = fs.readFileSync('./data/second.txt',);

console.log(first);
console.log(second);
console.log(second.toString());

const functions = require('./timers.js');


fs.writeFileSync('./data/firstjscreatedwithnode.js', )

